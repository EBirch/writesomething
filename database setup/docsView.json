{
   "_id": "_design/docs",
   "language": "javascript",
   "views": {
     "author": {
       "map": "function(doc) { emit(doc.auth, { 'id': doc._id, 'title': doc.title, 'type':doc.type, 'path':doc.path, 'date':doc.date }); }"
    }
  }
}
